//object literal
const producto = {
    nombre: 'monitor  20"',
    precio: 300,
    disponible: true,
    };


    console.log(Object.keys(producto));//metodo de los objectos, que retorna las llaves

    console.log(Object.values(producto));// metodo de los objectos, que retornas los valores del objecto

    console.log(Object.entries(producto));//metodo de los objectos. que retorna todos